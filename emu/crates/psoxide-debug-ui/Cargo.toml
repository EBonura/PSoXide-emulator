[package]
name = "psoxide-debug-ui"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Guest performance panel for the PSoXide frontends: per-vblank PS1 telemetry history and its egui charts."

[dependencies]
emulator-core = { workspace = true }
egui          = { workspace = true }

[lints]
workspace = true
