.claude/
/target
**/target
# sdk/ uses `target-dir = "../../../build/examples"` for MIPS builds.
/build
Cargo.lock.orig
*.swp
*.swo
.DS_Store
.vscode/
.idea/

# Local bios/game files — never commit
bios/
roms/
discs/

# Run artifacts
run/
traces/
goldens/*.bin
goldens/*.raw
